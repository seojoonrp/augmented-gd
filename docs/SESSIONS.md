# Sessions — what happened, in order

One entry per working session, oldest first. `STATUS.md` holds only the
current state; this is where the reasoning and the dead ends live. Dates are
absolute.

- **2026-09-15** — Environment set up (winget: git/cmake/ninja/LLVM/geode CLI; VS Build Tools already present). MSYS2 GCC on PATH broke the first build → CMakeLists defaults to clang on Windows. Project scaffolded, M2–M4 implemented.
- **2026-09-16** — 5 augments + gauge + HUD implemented. Fixed: anticheat-spike death counting, slow-mo via scheduler hook + FMOD master pitch, hitboxes drawn manually (GD's own path is byte-patch-only). Still open: hotkeys. Reference mod sources cloned into `refs/`.
- **2026-09-16 (harness)** — `docs/HARNESS-PLAN.md` phases 0–3 done: `refs/` re-selected for GD 2.2081 / Geode 5 (openhack dropped as stale; qolmod, xdbot, CBF, death-tracker, cleanstartpos, betterinfo, node-ids, devtools, example-mod, geode-docs added, commits pinned, `refs/MANIFEST.md` generated); scripts `bro.ps1` / `refgrep.ps1` / `nodeids.ps1` / `mods.ps1`; `docs/refs/INDEX.md` + per-ref pages; `docs/RECIPES.md`; GD-INTERNALS extended with the findings (mirror via `toggleFlipped`, checkpoint bindings, `m_isDead` death test, clear-counting flags, key delivery order, CBF/death-tracker interference). Phase 4 (hotkey fix through the harness) is the next in-game test. No code changed this session.
- **2026-09-16 (hotkey fix)** — Found the hotkey root cause by reading loader source (no guessing): the loader's keybind listener is registered before mod binaries load and returns `Stop` when Custom Keybinds' Z/X (practice checkpoint) listeners do, so our listener never ran in a level. Fix in `PlayLayerHook.cpp` + `mod.json`: settings `"priority": -5` + node-scoped `KeybindSettingPressedEventV3` listeners in `PlayLayer::init`, and a raw `KeyboardInputEvent` listener at priority -1 from `$on_mod(Loaded)`; per-frame dedup on the layer; `toggleSlowMo` / `tryPlaceCheckpoint` now return whether they consumed the key. Builds and installs; awaiting the in-game test above.
- **2026-09-16 (hotkeys verified)** — User confirmed X/Z work. Trimmed the per-key `Raw key press` log. Added: draft no longer pops on a checkpoint respawn (waits for the from-0 reset); debug number keys 1..N grant one level of the N-th augment (`AugmentManager::grant`, `AugPlayLayer::debugGrantAugment`, setting `debug-augment-keys`). Both untested in game.
- **2026-09-16 (later)** — Planning only, no code. Wrote `docs/ROADMAP.md`: goals, difficulty tiers (T1–T4 = number of unverified GD facts), ~20 candidate augments, systems, rejected list, build order. Recorded the bindings checked in `GD-INTERNALS.md`.
- **2026-09-16 (Blunt)** — 6th augment `blunt` (hazard hitboxes 80/60/40 %, test values). Harness path: `bro.ps1` for every binding (`getObjectRect` `win 0x1976a0`, `updateOrientedBox` `win 0x1a1570`, `orderCorners` `win 0x6dda0`; `getObjectRectPointer` / `getObjectRadius` / `computeAxes` are `win inline`), `refgrep` found qolmod AccurateHitboxes as the working precedent for editing OBB corners. New file `src/hooks/HazardHitboxHook.{hpp,cpp}` (global scale + two `GameObject` hooks), `PlayLayerHook.cpp` sets the scale before `PlayLayer::init` creates objects, scales `m_objectRadius` in `addObject`, `applyBlunt()` re-dirties every hazard when the level changes, `onQuit` resets. Docs: GD-INTERNALS "Object collision shapes", RECIPES, refs INDEX/qolmod, DESIGN, ROADMAP (player-side shrink stays rejected; hazard-side is a different path). Builds and installs; awaiting the in-game test above.
- **2026-09-16 (Blunt verified, Checkpoint rework)** — User: Blunt works. Checkpoint changed to "each placed checkpoint is one respawn" (placing allowed after a respawn while the budget lasts; newest first; from 0 when none left). Read GD's checkpoint internals from Geode's inline sources: `removePlacedCheckpoint` (delete a checkpoint placed < 0.1 s before death via `removeCheckpoint(false)`), `getLastCheckpoint` unguarded, `queueCheckpoint`; qolmod nulls `m_currentCheckpoint` before reset. New helpers `gdCheckpointCount` / `gdLastCheckpoint` / `syncCheckpointArray` / `consumeCheckpoint`; `CAN'T PLACE HERE` notice when GD refuses. "Sometimes doesn't trigger" still needs a log. Builds and installs; untested.
- **2026-09-17 (design table + Korean)** — Applied the user's 9-row design table. Data model: `AugmentDef { id, name, maxLevel, initialDesc, levelUpDesc, stub }`, `describe(level)` = initial for Lv1, level-up text otherwise. Ids now match the table (`slow-mo`, `startpos`, `hazard-hitbox`; C++ `ids::StartPos` / `ids::HazardHitbox`, namespace `augment::blunt` → `augment::hazard`, `bluntScale` → `hazardScale`, `applyBlunt` → `applyHazardScale`). Numbers: noclip 1.5 s, slow-mo/hazard 5 %/level as formulas (`tune::SlowMoStep` / `HazardStep`), startpos and hazard max 5. Stubs `wave-hitbox` / `nerve` / `draft-count` are draftable and recorded only (`tune::WaveStep`, `NerveMult {1.0, 1.5}`, `DraftCountCards` reserved). Korean UI: Pretendard SemiBold/Regular in `resources/fonts` → `mod.json resources.fonts` (Geode generates sd/hd/uhd bitmap fonts; `size` = UHD size), `scripts/fontcharset.ps1` builds the charset from string literals in `src/` and `build.ps1` runs it; `src/ui/Fonts.hpp` holds the `_spr` names; cards 140×180. Logs / notices / HUD wording stay English, augment names Korean. Builds and installs; everything above is untested in game.
- **2026-09-17 (stubs implemented)** — The last three stubs now do something; the pool is 9 working augments and `AugmentDef::stub` is gone. **wave-hitbox**: harness path first — `bro.ps1 GameObject getObjectRect` showed the `(float, float)` overload is `win 0x1976c0` (hookable, not inline) and `refgrep getObjectRect(` turned up `refs/qolmod/src/Hacks/Level/HitboxMultiplier.cpp:103-131`, which scales that overload's arguments for `typeinfo_cast<PlayerObject*>(this)` and registers it as a safe-mode trigger. That retires ROADMAP's "player hitbox shrink is inlined, rejected" entry. New `src/hooks/PlayerHitboxHook.{hpp,cpp}` (`augment::player`), gated on `m_isDart` (wave = start mode 4, confirmed in cleanstartpos/CBF/xdbot). Known gap: the player's oriented box, used when the *hazard* is rotated off-grid, is untouched — recorded in GD-INTERNALS. **nerve**: `AugmentManager::hazardScale/waveScale` now take a progress argument and are multiplied by `nerveBoost(progress) = 1 + NerveMult[lv-1] * progress`; both are floored at `tune::MinHitboxScale` (0.2) because wave Lv5 + nerve Lv2 would otherwise pass zero. `PlayLayerHook` grew `applyHitboxScales()` (exact, on reset / pick / debug grant) and `updateHitboxScales()` (per frame, re-applies the hazard scale only past a 0.01 deadband, since that walks `m_objects`). **draft-count**: `AugmentManager::draftCardCount()`, `rollDraft` no longer defaults to 3, and `AugmentDraftPopup::layoutFor()` narrows cards to `(540 - padding - gaps) / n` and scales their contents by `width / 140` so the wrap width in font units stays put and the text block gets shorter, not taller. HUD gained a line per new augment. Builds and installs; all three are untested in game.
- **2026-09-17 (stubs verified)** — User confirmed wave-hitbox, nerve and draft-count all work in game. Trimmed the verified diagnostics: per-object `HazardHitbox: rect of / oriented box of …` and per-reset `shrunk N rects` (plus the `Stats` counters in both hitbox hooks and `hazardRadiiAtLoad`), `WaveHitbox: player rect …`, `Draft popup: …`. Scale-change lines stay. Builds and installs.
- **2026-09-17 (card UI + baked fonts)** — *(rendered in game the same evening: fonts, cards, reveal all OK; only the description didn't wrap — see next entry)*  Draft cards restyled to GD's button look (white rim, `GJ_button_01` green body, black-outlined white text, footer band) with the layout from the user's reference (name / image box / description / footer), a centre fan-out reveal, and an overflow fix (description shrinks in steps until it fits its slot). Fonts: the UI font is now 아임크리수진 (`resources/fonts/ImcreSoojin.ttf`) with a **baked** black outline + drop shadow — Geode CLI 3.9.0 parses `outline` in `resources.fonts` but the implementation is commented out (`cli/src/util/bmfont.rs`), so `scripts/fontgen.py` (Windows Python + Pillow, `py -3 -m pip install pillow fonttools`) renders `AugName`/`AugText` sd/hd/uhd into gitignored `resources/fonts/gen/`, shipped via `resources.files`; `build.ps1` runs fontcharset → fontgen → geode build. Pretendard stays as `AugDebug` for HUD lines. The reveal is clock-driven from `visit()` because the director is paused during drafts. Builds and installs; nothing seen in game yet.
- **2026-09-17 (card v2, verified)** — In-game screenshot: everything rendered, but `CCLabelBMFont`'s width argument put each description on one line — and per the user it had never wrapped Pretendard either, so the width argument simply doesn't wrap mod fonts (GD-INTERNALS updated). Replaced with our own `wrapText()` (measure words with throwaway labels, insert `\n`, create the label with `kCCLabelAutomaticWidth`). Also: thinner rim, pips = current level, bigger title, tracking in `fontgen.py`, white image panel with radius-aware border, description centred in its gap. User confirmed all of it in game ("다 잘 된다"); card logs trimmed.
- **2026-09-17 (harness update)** — Docs only (the `GD bar child` dump had already gone with v2d): GD-INTERNALS "Progress bar" now carries the measured geometry (bar 210x16 at (284.5, 312), fill child at (2, 4) 8 high, colour (210,255,50), percent label font via `getFntFile()`); RECIPES gained "Decorate GD's progress bar" and "Twin of a GD node", the HUD recipe now says `m_uiLayer` + `CCLayerColor`; refs INDEX row for nodes created in `setupHasCompleted` (node-ids); DESIGN HUD section rewritten for the bottom gauge / dots; VISUAL-IDEAS 2.1 / 2.2 / 2.4 marked partly built; CLAUDE.md code map. No behaviour change.
- **2026-09-17 (HUD v2e)** — Gauge fill is the draft cards' `GJ_button_01` green (122, 222, 45), i.e. the mod's own UI colour (user first said "match the UI", then clarified: the card green, not GD's fill); progress marks are dots (radius = half the fill height, black rim, white best / green checkpoint) drawn in one `CCDrawNode` via `drawDot`, redrawn only on change. Builds; untested.
- **2026-09-17 (HUD v2d)** — v2c partly seen: progress marks rendered (best line poked out of the pill, gold). Per user: marks are now 1 pt white (best) / green (checkpoint) lines with a 1 pt black outline, exactly the fill's height and inside the track (GD log: bar 210x16, fill at (2, 4) 8 high, colour (210,255,50)). Gauge moved to the **bottom edge, mirrored** from GD's bar (`y = winH - bar.y`), readout is `charge/cost` (e.g. `23/40`) in a twin of `m_percentageLabel` (same fnt file, scale, mirrored position; `bigFont.fnt` 0.5 fallback); READY / gold removed. New `AugmentManager::pendingGaugeDrafts()` so only gauge-earned pending drafts show the bar full — the free opening draft no longer starts the bar full-then-draining. Builds; untested.
- **2026-09-17 (HUD v2c)** — User screenshot: v2b gauge was a giant white pill and the progress marks were missing. Log showed `ProgressMarks: no progress bar` — `m_progressBar` is null at `PlayLayer::init` for online levels (created in `setupHasCompleted`, node-ids does the same; recorded in GD-INTERNALS). Fix: `attachToProgressBar()` called from both `init` and a new `setupHasCompleted` hook; the gauge now copies GD's bar `displayFrame()` / scaleX-Y / anchor / colour instead of loading `GJ_progressBar_001.png` (340x20 white), fill position/height read from `m_progressFill`, plus a dark track behind the fill. Logs GD's bar children once (`GD bar child N: …`) so the look can be matched exactly next round. Builds; untested.
- **2026-09-17 (HUD v2b)** — User verified v2a in game. Per request: gauge moved under GD's progress bar as a horizontal bar built from the same sprite (`RunHud::attachGauge(m_progressBar, m_progressFill)`, scale/anchor/inset copied from GD's), augment column at the far left, icon boxes 12 pt. Builds and installs; untested.
- **2026-09-17 (HUD v2)** — First item from `VISUAL-IDEAS.md`: `src/ui/RunHud.{hpp,cpp}` rewritten (gauge thermometer bar via `CCLayerColor`s eased in `update()`, remaining-points label riding the fill top, header line, slot rows with placeholder icon boxes — `setGauge` / `setHeader` / `setSlots` replace `setLines`), new `src/ui/ProgressMarks.{hpp,cpp}` (best line + checkpoint ticks as children of `PlayLayer::m_progressBar`; track inset read from `m_progressFill`'s x when it is a child of the bar, else 2.5 pt guess, geometry logged once). `PlayLayerHook`: `Fields::checkpointPercents` mirrors `checkpoints`, `Fields::marks`, `refreshHud` builds slots. Builds and installs; untested.
- **2026-09-17 (visual brainstorm)** — Planning only, no code. Wrote `docs/VISUAL-IDEAS.md`: per-augment visual feedback ideas (constant / trigger / pick layers), system-level juice (gauge bar + floating `+N`, progress-bar markers, HUD icon row), a cost table of the available materials (CCCircleWave, GD particle plists, GhostTrailEffect, `m_waveTrail->m_waveSize`, player glow, `shakeCamera`, `playEffect`, ShaderLayer — all bindings checked with `bro.ps1`), a do-not list and a recommended build order. Nothing tested in game.
- **2026-09-17 (draft gauge v2)** — Gauge redesigned per user: flat floor removed (early-death farming), new-best delta bonus with a `NEW BEST +X` notice, threshold 40 rising 10 per gauge-earned draft, and a free opening draft. First shown on the level-info screen (rendered fine, user: "잘 된다") but the user wants it after the level loads, so it is now a pending draft queued by `startRun` and shown by a new `PlayLayer::startGame` hook (`bro.ps1`: win ok; death-tracker hooks it the same way). Debug key 0 (`debugFillGauge`) tops the gauge up to the threshold. Pending drafts are now a count (`m_pendingDrafts`): `onDeath` loops while the gauge covers the (rising) threshold, and `showDraft`'s pick callback opens the next popup while any remain, keeping the director paused and the saved cursor state until the last pick. Settings: `min-charge` / `draft-threshold` / `debug-augment-keys` replaced by `debug-mode` (number keys + fixed `debug-threshold`, `enable-if`). Numbers in `tune::Gauge*` / `tune::NewBestBonusMult`. Builds; untested in game.
- **2026-09-17 (cat)** — 10th augment `cat` (고양이, Lv1-5: 5+1/level hazards every 4-0.5/level s) per the user's table row; function only, cat UI/art deferred. Harness path: `bro.ps1` for `GJBaseGameLayer::destroyObject` (win ok), `deactivateObject`, `m_isDisabled`/`m_isDisabled2`, `setOpacity`; `refgrep` found xdBot disabling objects with `m_isDisabled = m_isDisabled2 = true` around `collisionCheckObjects`, and Geode's inline `GameObject::destroyObject()` is exactly those flags + `setOpacity(0)`, so that inline is what the cat calls. "In view" = `convertToWorldSpace` inside the window (+30 pt margin), "ahead" = object-layer x past the player (flipped when `m_isGoingLeft`); only Hazard / AnimatedHazard (`hazard::isTarget`). Per-attempt `catTimer` + `catRemoved` (obj + previous opacity) in `Fields`; `catRestore()` before every `resetLevel`. Foresight's sorted list became `objectsByX()` (shared) and skips disabled objects. Debug keys: Shift+1..9 = augments 10+. Docs: GD-INTERNALS "Removing an object at runtime", refs INDEX row, DESIGN row. Builds and installs; untested in game. Then `src/ui/CatNode.{hpp,cpp}`: placeholder square in the bottom-right corner of `m_uiLayer` (created on the first `catTick`), `fireAt(targets)` = scale pop + lasers drawn each frame in screen space from the square to `parent->convertToWorldSpace(obj pos)`, fading over 0.45 s. Also untested.
- **2026-09-17 (refactor 0-1)** — Plan in `docs/REFACTOR-PLAN.md`. Harness: `scripts/check.ps1` (audits every `$modify` override against the bindings — all 14 are `win ok` — and checks doc → source cites), `scripts/diag.ps1` (log-line inventory), `scripts/test.ps1` (host tests, run by `build.ps1`). Code: `src/core/` is now Geode-free (`AugmentDef` + `Formulas.hpp` + `RunState`), `AugmentManager` moved to `src/game/` keeping its API; descriptions built from `tune::`. `tests/core_tests.cpp`: 582 checks. Builds and installs; smoke test pending (row above).
- **2026-09-17 (refactor 2a)** — Step 1 verified in game ("다 괜찮아"). Then the augment-module split: `src/augments/{Augment.hpp,Augments.*,Shield,SlowMo,Foresight,Unmirror,DraftCount,HitboxScales}.cpp`, `src/game/{LevelSession,DraftSession,Scales}.*`, `src/input/Hotkeys.*`, `src/hooks/SchedulerHook.cpp`; `PlayLayerHook.cpp` 1065 → 470 lines (lifecycle + startpos + cat + HUD refresh). `AugmentManager` lost the pause/cursor state (now `draft::`), gained `beginLevel/endLevel/session()`. The session is created *before* `PlayLayer::init` (objects created inside init must see the hazard scale) and the hook looks it up by layer pointer (`sessionFor`) because `PlayLayer::get()` may not be set yet at that point. Builds and installs; regression pending (row above).
- **2026-09-17 (refactor 2b–4 + docs)** — 2a verified in game ("괜찮음"). 2b: `StartPos.cpp` (checkpoint model, `onBeforeReset` returns "this reset is a respawn" and flips practice mode around GD's reset, `onAttemptStart` consumes; only touches GD's checkpoint array on run attempts, so practice mode on a run level keeps the player's own checkpoints) and `Cat.cpp`; `refreshHud` moved into `LevelSession` (10 Hz text rebuild, gauge fill still eases per frame); `hotkeys::legacyRoute` gone; `PlayLayerHook.cpp` is 170 lines of lifecycle. Step 4: unmirror is now a `GJBaseGameLayer::toggleFlipped` hook (refuses `flip = true` on a run attempt that owns it; drafting calls `toggleFlipped(false, true)` once, the call cleanstartpos / miscbugfixes make after every reset) — the object surgery is gone. Docs: STATUS reduced to state, this file holds the history, `HARNESS-PLAN.md` → `HARNESS.md`, CLAUDE.md rewritten (rule 5 now says "ask session()", rule 11 = cite by symbol), CI → Windows-only with font baking (unverified). `check.ps1` also understands `File.cpp::symbol`. Builds and installs; 16 hooks `win ok`; regression pending.
- **2026-09-17 (brake)** — 11th augment `brake` (브레이크, Lv1-3) from the user's table row: hold C → 40 % game speed, `7·level` real seconds per attempt; per the user slow-mo is ignored (absolute 40 %, not stacked). Two plumbing changes: `scales::setTimeOverride` (brake) layered over `scales::setTime` (slow-mo base) so the two augments stop overwriting each other every frame, and `hotkeys::route` / `Augment::onHotkey` now carry `down` — the raw listener forwards `Release` (skips `Repeat`), the setting listener for `keybind-brake` forwards both edges, releases bypass the draft-open guard, the per-frame dedup is per edge. Budget refills on from-0 resets only (shield's rule), a grant mid-attempt adds its 7 s, `onPause` forgets the held key (focus loss sends no release). Debug key Shift+2. Builds and installs; untested in game.
- **2026-09-17 (checkpoint v3)** — Per the user after testing: the "each checkpoint is one respawn, newest first" chain felt loose, so `StartPos.cpp` now keeps a single live `Ref<CheckpointObject>` — placing again moves it (`CHECKPOINT MOVED`, the one green dot moves), a death respawns there once, and the next death restarts from 0 unless a new one was placed after the respawn (assumption: the placement budget is still `level` per from-0 life). `syncGdArray` shrank to "GD's last entry must be the target". Second request: checkpoints save state — new `Augment::onCheckpointPlaced` / `onCheckpointRespawn` fanned out by `LevelSession` (the latter right after `onAttemptStart(true)`, so it is independent of table order); shield snapshots `m_used`, brake snapshots `m_left`. One slot per augment suffices because only the newest checkpoint is ever respawned at. Builds and installs; untested in game.
- **2026-09-17 (refactor 2b + 4 verified)** — User: "다 잘 되는듯". Startpos / cat modules, session-owned HUD refresh, the `toggleFlipped` unmirror hook (on portal-free levels) and the 10 Hz HUD all confirmed; `REFACTOR-PLAN.md` closed except the optional items. Host tests 594/594, `check.ps1` 0/0.
- **2026-09-17 (brake + checkpoint v3 verified)** — User: "잘 된다 굳". Brake, the press/release hotkey path, the time-override layer and the checkpoint snapshots are all confirmed in game; STATUS / DESIGN / GD-INTERNALS marked accordingly. Diagnostics kept (one line per press/release/placement, none per frame).
