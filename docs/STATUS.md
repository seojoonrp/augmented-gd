# Status

Update this file at the end of every working session: what was verified in
game, what is broken, what to try next. Dates are absolute.

## Verified working in game (2026-09-16)

- Build → auto-install → mod visible in Geode mod list.
- `AUG` button on LevelInfoLayer, Start / Preview / Continue / Restart flow.
- Death counting, draft gauge, draft popup on respawn, mandatory pick, cursor
  shown during draft and hidden again afterwards.
- Shield: hit consumed, 3 s noclip, dies again after; anticheat-spike call
  passes through (no more phantom deaths / instant shield loss).
- Slow-Mo: game speed and music slow down together (default ON after pick).
- Foresight: GD-style hitboxes (user: "perfect").
- HUD lines and notices.
- Hotkeys X (Slow-Mo toggle) / Z (Checkpoint place) — user confirmed after the
  2026-09-16 fix (loader listener order + Custom Keybinds Z/X, see GD-INTERNALS).
- hazard-hitbox (then "Blunt", hazard hitboxes 80/60/40 %): user confirmed it
  works (2026-09-16, "잘 된다"). Now 5 %/level, see below.
- Checkpoint: placing (Z) and a single respawn work (user report 2026-09-16).

## Broken / unverified

| Item | State | Evidence | Next step |
|---|---|---|---|
| Draft after a checkpoint respawn | Fixed, untested: `resetLevel` skips the draft when `fromCheckpoint`, so it waits for the reset that starts from 0. | User report 2026-09-16 | Place checkpoint, die with the gauge full → respawn with no popup; die again → popup on the fresh attempt. |
| **Korean text / Pretendard fonts** | Built 2026-09-17, not yet seen in game. `AugName` (SemiBold, 96) / `AugText` (Regular, 64) generated by Geode from `resources/fonts`, 227 glyphs each, all three resolutions in the package (checked by unzipping the .geode). Used by the draft cards, popup title, HUD lines and notices. | Package contents only. | Open a draft: title "증강 선택", card names / descriptions in Korean, nothing blank. Blank text = font not found (log would show a cocos "Cannot load" / check `resources/selenophile.augmented-gd/AugName-uhd.fnt` in the Geode install). Name letter-spacing uses `setExtraKerning(-1)` (unverified unit) — if names look cramped or spaced out, change `fonts::NameKerning`. |
| Draft card layout | Cards grew to 140×180 pt for the Korean descriptions (name 0.55, desc 0.55 scale, wrap at 124 pt). Untested. | — | Check the longest texts fit: 스타트포스 Lv1, 청심환 Lv1. If a description overflows the card bottom, lower `kDescScale` in `AugmentDraftPopup.cpp`. |
| New numbers (design table 2026-09-17) | Untested: noclip 1.5 s, slow-mo 5 %/level (95/90/85), startpos max 5, hazard-hitbox 5 %/level (95…75). | — | Grant with debug keys and read the HUD (`나무늘보 Lv1: ON (95%)`, `위협제거 Lv1: hazards 95%`); shield noclip timer counts from 1.5. |
| Debug augment keys 1–9 | Untested as such. Raw key path; setting `debug-augment-keys` (default on). Order: shield, slow-mo, startpos, foresight, unmirror, hazard-hitbox, wave-hitbox, nerve, draft-count. | — | In a run press 1..9 → HUD "+이름 LvN (DEBUG)", log `Debug grant: 'id' -> level N`; maxed → "MAXED" notice. Slow-Mo applies speed immediately, Unmirror runs `applyUnmirrorNow`, and 6–8 (hazard-hitbox / wave-hitbox / nerve) run `applyHitboxScales`. |
| **Checkpoint — "each placement = one respawn" model** | Rewritten 2026-09-16, untested. Own `std::vector<Ref<CheckpointObject>>`; before a checkpoint reset GD's array is rebuilt if it diverged (`syncCheckpointArray`) and `m_currentCheckpoint` set; after the reset `removeCheckpoint(false)` consumes the used one (`consumeCheckpoint`). Placing stays allowed after a respawn while the attempt budget lasts. | User 2026-09-16: after a respawn they could not place again (old `respawnUsed` rule); and "sometimes a placed checkpoint doesn't trigger" — cause unknown, **log needed**. Likely candidate: GD's `removePlacedCheckpoint` deletes a checkpoint placed < 0.1 s before the death and the old fallback left `m_currentCheckpoint` unset. | Lv2: place, die → respawn there; place again → OK; die → respawn at the new one; die → respawn at the first one; die → from 0, budget refilled. Watch the log: `Checkpoint placed (n/lvl), k ready, GD array m`, on death `Checkpoint: death with … -> respawn`, then `Checkpoint: rebuilt GD array` (only if GD dropped entries), `Respawned from checkpoint (…)`, `Checkpoint: consumed (GD array a -> b)` with b == a-1 and no `still on top!`. If the player is teleported right after respawning, `removeCheckpoint(false)` also resets → switch consumption to hiding the marker + rebuild at the next death. Repro the "didn't trigger" case and paste `scripts/logs.ps1`: `markCheckpoint returned null` now also shows a `CAN'T PLACE HERE` notice. |
| **wave-hitbox (웨이브브레이커)** | Built 2026-09-17, untested. New hook `PlayerHitboxHook.cpp` on the `getObjectRect(float, float)` overload (`win 0x1976c0`), arguments scaled by `1 - 0.10*level` when `typeinfo_cast<PlayerObject*>(this)` and `m_isDart`. | qolmod HitboxMultiplier is the precedent and declares it a safe-mode trigger, i.e. it changes real collision. | Grant with debug key 7 on a level with a wave section. HUD: `웨이브브레이커 Lv1: player 90% in wave` plus `ACTIVE` while in wave. Log on entering wave: `WaveHitbox: player rect (factor …) -> WxH` (2 lines per scale change) and per reset `WaveHitbox: shrunk N player rects`. **N == 0 while a wave section was played = the hook is not on the collision path** → next approach is `GJBaseGameLayer::checkCollisions`. If it works against spikes but not against *rotated* hazards, that is the player-OBB gap (`GD-INTERNALS.md` "Player collision shape"). |
| **nerve (청심환)** | Built 2026-09-17, untested. Both hitbox shrinks are multiplied by `1 + k*progress` (k = 1.0 / 1.5), recomputed every frame; the player scale follows exactly, the hazard scale is re-applied only once it drifts `kHazardReapplyStep` (0.01) because that walks every object. | — | Grant 위협제거 + 청심환 (keys 6, 8) and watch the HUD line `청심환 Lv1: shrink x1.00 → x1.50` climb with the percent, and `위협제거 Lv1: hazards 95% → 93%`. Log: repeated `HazardHitbox: scale … applied to N hazards` during an attempt. If the game stutters at those moments, raise `kHazardReapplyStep`. |
| **draft-count (기회비용)** | Built 2026-09-17, untested. `rollDraft(mgr.draftCardCount())`; popup narrows cards to 114 pt and scales their text by 114/140 so four fit in 540 pt. | — | Grant with debug key 9, then take a draft: 4 cards, none clipped by the popup edge, descriptions not overflowing the card bottom. Log: `Draft popup: 4 cards, 114x180 each, popup 540x234`. If text overflows, lower the `scale` ratio in `layoutFor`. |
| Unmirror | Unverified — no mirror-portal level tested. | Log said "neutralized 0 mirror portal(s)" on Retention (has none). | Replace object surgery with a `GJBaseGameLayer::toggleFlipped` hook that returns early (qolmod/xdBot pattern, `docs/refs/INDEX.md`). Then test on a mirror level. |
| Slow-Mo with Click Between Frames | Unverified interaction. | CBF (installed) derives physics steps from `CCDirector` deltas; qolmod scales those too when CBF is loaded. | If Slow-Mo feels wrong, test with CBF disabled; if that fixes it, scale `m_fActualDeltaTime`/`m_fDeltaTime` as qolmod does. |
| Draft pending when re-entering a level | Handled (`isRunning()` guard) but not tested. | — | Quit with a draft pending, re-enter, die once → popup should appear then. |

## Diagnostic logging currently in the code

These `log::info` lines are intentional and should stay until the feature is
verified, then be trimmed:
- `Debug grant: …` — number keys
- `Hotkey raw listener registered` / `Hotkey setting listeners attached to PlayLayer`
- `Hotkey X via raw|setting (frame N)` / `… duplicate in frame N, ignored` / `… ignored: draft open|no PlayLayer`
- `X ignored: …` / `Z ignored: …` — hotkey handlers' early returns
- `Foresight: tracking N objects`
- `Game speed -> …`
- `Death #N at …% -> +charge, gauge …` — keep (useful for tuning)
- `HazardHitbox: hazard hitbox scale -> …` / `HazardHitbox: scale … applied to N hazards (M circular)` — keep
- `HazardHitbox: rect of object id …` / `HazardHitbox: oriented box of object id …` (first 4 after each scale change) and `HazardHitbox: shrunk N rects, M oriented boxes since the last reset (…)` per reset — the mechanic is verified; trim these next session
- `WaveHitbox: player hitbox scale -> …` / `WaveHitbox: player rect …` (2 per scale change) / `WaveHitbox: shrunk N player rects since the last reset` — keep until wave-hitbox is verified
- `Draft popup: N cards, WxH each, popup WxH` — keep until the 4-card layout is verified
- `Checkpoint placed (n/lvl), k ready, GD array m` / `markCheckpoint returned null at …%` / `Checkpoint: death with …` / `Checkpoint: respawn dropped …` / `Checkpoint: rebuilt GD array …` / `Checkpoint: consumed (GD array a -> b…)` / `Respawned from checkpoint (…)` — keep until the multi-respawn model is verified

## Session log

- **2026-09-15** — Environment set up (winget: git/cmake/ninja/LLVM/geode CLI; VS Build Tools already present). MSYS2 GCC on PATH broke the first build → CMakeLists defaults to clang on Windows. Project scaffolded, M2–M4 implemented.
- **2026-09-16** — 5 augments + gauge + HUD implemented. Fixed: anticheat-spike death counting, slow-mo via scheduler hook + FMOD master pitch, hitboxes drawn manually (GD's own path is byte-patch-only). Still open: hotkeys. Reference mod sources cloned into `refs/`.
- **2026-09-16 (harness)** — `docs/HARNESS-PLAN.md` phases 0–3 done: `refs/` re-selected for GD 2.2081 / Geode 5 (openhack dropped as stale; qolmod, xdbot, CBF, death-tracker, cleanstartpos, betterinfo, node-ids, devtools, example-mod, geode-docs added, commits pinned, `refs/MANIFEST.md` generated); scripts `bro.ps1` / `refgrep.ps1` / `nodeids.ps1` / `mods.ps1`; `docs/refs/INDEX.md` + per-ref pages; `docs/RECIPES.md`; GD-INTERNALS extended with the findings (mirror via `toggleFlipped`, checkpoint bindings, `m_isDead` death test, clear-counting flags, key delivery order, CBF/death-tracker interference). Phase 4 (hotkey fix through the harness) is the next in-game test. No code changed this session.
- **2026-09-16 (hotkey fix)** — Found the hotkey root cause by reading loader source (no guessing): the loader's keybind listener is registered before mod binaries load and returns `Stop` when Custom Keybinds' Z/X (practice checkpoint) listeners do, so our listener never ran in a level. Fix in `PlayLayerHook.cpp` + `mod.json`: settings `"priority": -5` + node-scoped `KeybindSettingPressedEventV3` listeners in `PlayLayer::init`, and a raw `KeyboardInputEvent` listener at priority -1 from `$on_mod(Loaded)`; per-frame dedup on the layer; `toggleSlowMo` / `tryPlaceCheckpoint` now return whether they consumed the key. Builds and installs; awaiting the in-game test above.
- **2026-09-16 (hotkeys verified)** — User confirmed X/Z work. Trimmed the per-key `Raw key press` log. Added: draft no longer pops on a checkpoint respawn (waits for the from-0 reset); debug number keys 1..N grant one level of the N-th augment (`AugmentManager::grant`, `AugPlayLayer::debugGrantAugment`, setting `debug-augment-keys`). Both untested in game.
- **2026-09-16 (later)** — Planning only, no code. Wrote `docs/ROADMAP.md`: goals, difficulty tiers (T1–T4 = number of unverified GD facts), ~20 candidate augments, systems, rejected list, build order. Recorded the bindings checked in `GD-INTERNALS.md`.
- **2026-09-16 (Blunt)** — 6th augment `blunt` (hazard hitboxes 80/60/40 %, test values). Harness path: `bro.ps1` for every binding (`getObjectRect` `win 0x1976a0`, `updateOrientedBox` `win 0x1a1570`, `orderCorners` `win 0x6dda0`; `getObjectRectPointer` / `getObjectRadius` / `computeAxes` are `win inline`), `refgrep` found qolmod AccurateHitboxes as the working precedent for editing OBB corners. New file `src/hooks/HazardHitboxHook.{hpp,cpp}` (global scale + two `GameObject` hooks), `PlayLayerHook.cpp` sets the scale before `PlayLayer::init` creates objects, scales `m_objectRadius` in `addObject`, `applyBlunt()` re-dirties every hazard when the level changes, `onQuit` resets. Docs: GD-INTERNALS "Object collision shapes", RECIPES, refs INDEX/qolmod, DESIGN, ROADMAP (player-side shrink stays rejected; hazard-side is a different path). Builds and installs; awaiting the in-game test above.
- **2026-09-16 (Blunt verified, Checkpoint rework)** — User: Blunt works. Checkpoint changed to "each placed checkpoint is one respawn" (placing allowed after a respawn while the budget lasts; newest first; from 0 when none left). Read GD's checkpoint internals from Geode's inline sources: `removePlacedCheckpoint` (delete a checkpoint placed < 0.1 s before death via `removeCheckpoint(false)`), `getLastCheckpoint` unguarded, `queueCheckpoint`; qolmod nulls `m_currentCheckpoint` before reset. New helpers `gdCheckpointCount` / `gdLastCheckpoint` / `syncCheckpointArray` / `consumeCheckpoint`; `CAN'T PLACE HERE` notice when GD refuses. "Sometimes doesn't trigger" still needs a log. Builds and installs; untested.
- **2026-09-17 (design table + Korean)** — Applied the user's 9-row design table. Data model: `AugmentDef { id, name, maxLevel, initialDesc, levelUpDesc, stub }`, `describe(level)` = initial for Lv1, level-up text otherwise. Ids now match the table (`slow-mo`, `startpos`, `hazard-hitbox`; C++ `ids::StartPos` / `ids::HazardHitbox`, namespace `augment::blunt` → `augment::hazard`, `bluntScale` → `hazardScale`, `applyBlunt` → `applyHazardScale`). Numbers: noclip 1.5 s, slow-mo/hazard 5 %/level as formulas (`tune::SlowMoStep` / `HazardStep`), startpos and hazard max 5. Stubs `wave-hitbox` / `nerve` / `draft-count` are draftable and recorded only (`tune::WaveStep`, `NerveMult {1.0, 1.5}`, `DraftCountCards` reserved). Korean UI: Pretendard SemiBold/Regular in `resources/fonts` → `mod.json resources.fonts` (Geode generates sd/hd/uhd bitmap fonts; `size` = UHD size), `scripts/fontcharset.ps1` builds the charset from string literals in `src/` and `build.ps1` runs it; `src/ui/Fonts.hpp` holds the `_spr` names; cards 140×180. Logs / notices / HUD wording stay English, augment names Korean. Builds and installs; everything above is untested in game.
- **2026-09-17 (stubs implemented)** — The last three stubs now do something; the pool is 9 working augments and `AugmentDef::stub` is gone. **wave-hitbox**: harness path first — `bro.ps1 GameObject getObjectRect` showed the `(float, float)` overload is `win 0x1976c0` (hookable, not inline) and `refgrep getObjectRect(` turned up `refs/qolmod/src/Hacks/Level/HitboxMultiplier.cpp:103-131`, which scales that overload's arguments for `typeinfo_cast<PlayerObject*>(this)` and registers it as a safe-mode trigger. That retires ROADMAP's "player hitbox shrink is inlined, rejected" entry. New `src/hooks/PlayerHitboxHook.{hpp,cpp}` (`augment::player`), gated on `m_isDart` (wave = start mode 4, confirmed in cleanstartpos/CBF/xdbot). Known gap: the player's oriented box, used when the *hazard* is rotated off-grid, is untouched — recorded in GD-INTERNALS. **nerve**: `AugmentManager::hazardScale/waveScale` now take a progress argument and are multiplied by `nerveBoost(progress) = 1 + NerveMult[lv-1] * progress`; both are floored at `tune::MinHitboxScale` (0.2) because wave Lv5 + nerve Lv2 would otherwise pass zero. `PlayLayerHook` grew `applyHitboxScales()` (exact, on reset / pick / debug grant) and `updateHitboxScales()` (per frame, re-applies the hazard scale only past a 0.01 deadband, since that walks `m_objects`). **draft-count**: `AugmentManager::draftCardCount()`, `rollDraft` no longer defaults to 3, and `AugmentDraftPopup::layoutFor()` narrows cards to `(540 - padding - gaps) / n` and scales their contents by `width / 140` so the wrap width in font units stays put and the text block gets shorter, not taller. HUD gained a line per new augment. Builds and installs; all three are untested in game.
