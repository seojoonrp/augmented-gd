# example-mod — geode-sdk/example-mod

Pinned `86696b8` (2026-04-13). The official template: `src/main.cpp` hooks
`MenuLayer::init` and adds one button. Use it as the baseline for file layout,
`CMakeLists.txt`, and `mod.json` fields when something in ours looks odd.
